import alias from '../alias.json'

export const ALIAS = alias

// Prescribed order of types for dropdown
export const ALIAS_TYPES = [
  alias.TYPES.COMMON,
  alias.TYPES.SYNONYM,
  alias.TYPES.FORMER,
]
