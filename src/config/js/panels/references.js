import button from '@/config/text/panels/references/button.json'
import panel from '@/config/text/panels/references/panel.json'
import tooltip from '@/config/text/panels/references/tooltip.json'

export const REFERENCES = {
  BUTTON: button,
  PANEL: panel,
  TOOLTIP: tooltip,
}
