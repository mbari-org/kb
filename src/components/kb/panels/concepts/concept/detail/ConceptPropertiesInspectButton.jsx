import InspectButton from '@/components/common/InspectButton'

const ConceptPropertiesInspectButton = ({ onClick, tooltip }) => (
  <InspectButton onClick={onClick} size={18} tooltip={tooltip} />
)

export default ConceptPropertiesInspectButton
