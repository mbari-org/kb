import TextContent from '@/components/modal/content/TextContent'

const LogoutContent = () => (
  <TextContent text='Using the browser back button effectively closes this app. Are you sure you want to logout?' />
)

export default LogoutContent
