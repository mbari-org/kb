import LoadingBackdrop from "./LoadingBackdrop"
import ModalError from "./ModalError"

const Modals = () => {
  return (
    <>
      <LoadingBackdrop />
      <ModalError />
    </>
  )
}

export default Modals
